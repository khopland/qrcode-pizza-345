---
import MenuPageView from '../components/MenuPageView.astro';
import {getMenu} from '../lib/sanity/content';

export const prerender = true;
const menu = await getMenu('daily');
---

<MenuPageView menu={menu} displayTitle="Menú del día · Fin de semana" />
