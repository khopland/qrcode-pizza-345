---
import HomePageView from '../components/HomePageView.astro';
import {getAllMenus} from '../lib/sanity/content';

export const prerender = true;
const menus = await getAllMenus();
---

<HomePageView menus={menus} />
